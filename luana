const asciify = require('asciify-image');
asciify('/home/matheusmr/Pictures/pp.jpeg', {
width: 100,
height: 100,
fit: 'box'
})
			.then((asciified) => {
				console.info('aeho');
				console.info(asciified);
				
			})
			.catch((err) => {
				// Print error to console
				console.error(err);
				done();
			});
